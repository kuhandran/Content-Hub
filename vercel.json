{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "npm run frontend:build && rm -rf .next && cp -r apps/frontend/.next .next",
  "installCommand": "npm ci --workspaces --include=dev"
}








